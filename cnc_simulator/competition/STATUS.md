# Competition build — active, not delivered

User objective: a compelling truthful demo/video of the agreed loops, multi-run improvement, W&B evaluations/monitoring/signals, ARIA outer review, working execution environments/sandboxes, raw live CAM/check/memory/prompt/simulation output, and impressive parts. No claim of completion or prize guarantee.

## Verified this pass

- Fetched GitHub. Joel's latest `origin/toukoversion` is `6571f36`. Read his ARIA review and product context; copies retained in `references/`. Did not merge over active Fusion work.
- W&B team project `silta/coreweave-hack-silta-squad` accessible through API and browser.
- Published actual retained programmatic five-part dataset and four reference/best/cold/warm evaluations. Every evaluation read back completed without exception. Exact links in `weave-publication.json`. This is retained evidence, not a new experiment.
- Existing five-part cold/warm comparison is mixed (2 warm faster, 2 slower); no general learning success claim.
- Hosted cwsandbox smoke executed a real creation attempt and failed `project not found`; `sponsor-access.json`. Normal W&B project name resolves through API. Organization enablement not yet proven; do not equate this error to a definitive entitlement verdict.
- ARIA request sent in browser tab 3, querying exactly this dataset/evaluations and proposing experiment/signals. It is actively running shell queries. Retrieve its final answer and save evidence before implementing recommendations.

## Remaining required work

1. Finish ARIA review -> select measured intervention -> execute experiment -> evaluate/retain or reject -> send actual results back to ARIA. Preserve outer-loop provenance.
2. Stronger failure-derived check evaluation: actual failure, saved check, valid counterexamples, changed setup bounds; fixed source and dataset hashes. Include false rejection, coverage, avoided full simulations; machining time remains sole plan objective.
3. Verify sandbox access using official setup and exact entity/project. If unavailable, retain concrete failure and honest fallback boundary; don't call local subprocess a security sandbox.
4. Wire new live simulator run tracing and useful monitoring/signals in W&B. Read back actual calls/signals, not just scaffold or historical eval publication. Existing other-team/catalog traces must not be attributed to this engine.
5. Raw live workflow: inspect generated strategy/movements, checks, frozen CAD, memory version/diff, prompt and timing with source links. Keep simple software; no dashboard clutter or staged progress.
6. Capture actual impressive indexed machining plus live/simple job; edit an under-two-minute cohesive film and a three-minute rehearsal. Verify video visually, audio/timing, all numbers and source provenance. No fabricated run counts or improvements.
7. Rehearse multiple end-to-end runs, finalize evidence summary and submission assets. Preserve known coarse 3 mm tolerance and 3+2 limitations.

## Runtime handles at this checkpoint

- W&B publication process completed successfully (exec 61750).
- Sandbox probe process completed (exec 73066).
- Local service remains 127.0.0.1:2744, exec 83956.
- CUA `sponsorTab`, tab 3, W&B /weave/agents, ARIA sidebar active.
- CUA `qa`, tab 2, completed new baseline run `02de8c83d5bd`.

## Overnight checkpoint (latest)

Previous work was concrete progress, not stalled: fresh counterexample audit completed, hosted checks executed, fresh Weave verification recorded, ARIA ablation completed, first-cut film encoded.

- `failure-memory-eval/result.json`: 5 fresh simulations; 3 invalid, 2 valid; memory catches 2 invalid exact repeats, zero false rejections; changed-fixture invalid correctly left for simulator. Published cold/memory evaluations read back; `failure-eval-publication.json`.
- `sponsor-minimal-access.json`: actual hosted execution succeeded after omitting entity/project headers. This supersedes initial failed probe, which remains preserved.
- `hosted-checks.json`: actual hosted execution of frozen exact-key membership over those five cases, network ingress/egress denied, no guest credentials; completed Weave trace. Key computation remains trusted host-side; don't claim arbitrary generated code validation.
- `aria-review.md`: completed actual ARIA review. Selected cleanup ablation, no blind rule promotion.
- `aria-ablation/result.json`: 12 fresh runs complete. Manifold cleanup off fails at .708/.75/.95; on passes 158.98s. Cage .75 passes196.77s, .83 passes198.38s, .95 fails, independent of cleanup toggle. Thus cleanup is not a universal repair.
- `publish_ablation.py` active publication handle1274; poll, don't restart. Must return actual results to ARIA next.
- `live-verification/receipt.json`: fresh reference/best simulations with nested Weave verification and computed monitoring outputs, server-readback confirmed. These outputs are NOT yet configured Weave UI Signals; no claim otherwise.
- `film/silta-evidence-first-cut.mp4`: 109.96s encoded with actual 3D machining captures and simple captions. Needs real W&B/ARIA and live-output capture, visual full-film review, final soundtrack/narration decision. It is a first cut, not final.
- `live_terminal.py`: read-only actual job monitor; verified against completed job02de8c83d5bd. Need use during a genuinely fresh live job, not label prior-state readout a live generation.
- Overnight heartbeat `cnc-demo-overnight-build` created every30min in this thread, ending preparation window Sept13 08:00 local. It is a fallback, not a replacement for current active work.

Priorities: close actual ARIA feedback loop; configure and verify useful Weave Signals; live trace integration in service rather than only standalone verification; raw live recording and polished final film; repeated rehearsals and comprehensive final audit. Do not stop at this checkpoint or call the goal complete.

Follow-up: ablation publication finished and read back (`aria-ablation-publication.json`). Actual results returned to the recovered ARIA conversation in CUA `sponsorReview`, tab4, evaluations page; second review running. Tab marked handoff. Seven evaluations now exist, so update the film's six-evaluations caption before final delivery. W&B default evaluation table currently has unrelated agent_correctness columns resulting in N/A for our cohorts; set an appropriate view or show individual evaluation before filming. Do not misinterpret N/A column mismatch as failed publication.

Latest active-work checkpoint:
- User explicitly wants continuous active work, no timed rest. Heartbeat cnc-demo-overnight-build PAUSED via app tool; do not reactivate unless asked. Active goal remains unfinished.
- Live service restarted under scoped hsec, exec4653. New `camloop/telemetry.py` traces actual live stages. `competition/serve_traced.py` is launcher; no credentials passed to Astra worker due its clean environment.
- Fresh traced model-driven PDF job `a1b399e521ac` completed:125.5s reference,27.3s best,78.3% reduction;memory9->10;two passing attempts;Weave root01a0993c-b389-7354-9903-8fb994b6912f readback complete. `fresh-live-rehearsal.json`, actual timestamped `fresh-live-terminal.jsonl`. Browser qa/tab2 shows new result and working trace link.
- Added optional observability dependency weave0.53.9 and tracing fail-open tests (tracing failure never changes verifier outcome). Tests4 signals/telemetry pass; previous live4pass.
- Added `camloop/signals.py`, exact certainty diagnostic and paired-coverage checks. These edits were made AFTER current server imported telemetry; restart only after confirming no live jobs before testing current signals. They are trace outputs, not yet Weave UI Signals.
- ARIA second review running in sponsorReview/tab4. Retrieve final narrow advisory, save response, implement only tested scope.
- `render_terminal_segment.py` creates20s edited true event recording; inspected generated-CAM frame. Waiting-time compression explicitly labeled. `build_film.py` now includes this plus7-eval caption; latest rebuild exec84633. Full resulting film still requires review and W&B/ARIA actual visual capture, not yet final.

Latest continuation checkpoint:
- Film now `film/silta-demo.mp4`,115.96s3479frames1080p, decodechecked. Actual Weave/ARIA UI screenshots replace sponsor textcard; later machining clips reveal finished features. Silent, narrative REHEARSAL.md. START_HERE.md is current handoff.
- Found and fixed Weave EvaluationLogger aggregate omission: auto_summarize=True in all three publishers. Republished same datasets, original receipts preserved initial-publications. Best dashboard visuallyverified5/5passes+machining169.9avg. NewIDs in receipts/START_HERE. These are corrected publications, not new experiments.
- ARIA followup completed, actual text saved aria-followup-response.txt. Narrow adjudicated advisory aria-advisory.md. Hashaudit proves12settings=5distinctpaths; manifold3stepssamepathwithincleanupbranch; cagecleanupno-op. .5mm audit hitresource limit (resolution-audit/result.json), no resolutionclaim.
- Telemetry closes errorpaths; planning/check/simulation/judge stages record owncandidate outputs. New regression test. 21stages of actual freshrepeat server-readback confirmed in rehearsal-02/trace-readback.json.
- Freshrepeat job056866dee37c completed3passing attempts,27.264best unchanged,memory10->11. Freshdifferentpart a2ff5d26d533 completed25.852s vs80.745ref,memory11->12,duplicatesafestop. Both actualAstra+trace. Rehearsal-02/-03 retain fulltapes. live-rehearsal-summary.json collects3freshtracedjobs.
- Sidebar API optional?summary=1+JS fetchselectedonlywhenupdated; unchangedrowsstaystable, staleasyncjob guarded. Measured350458->1436bytes list. Browser verifiedswitchtorunningbearingandcompletion.
- Fullsuite68passed before1newregression; latesttargetedlive+telemetry7passed,node--checkpassed. No geometrycodechanged.
- Currentserver exec4435 alive hsecscopedport2744. Alljobscomplete. qa tab2 currentbearing a2ff markedDeliverable. sponsorNow tab5 Signals page markedHandoff.
- ActualSignalsUI found under Agents>Signals. Existing1active signal,0volume,belongsotherwork. Newsignal>CustomSignal offersServerlessInference orManageproviders/runtimes; no exposedAstrachoice. Escclosedcreation,nonecreated. Need configuredAstra/customruntime or explicitmodelchange beforeclaimingUISignals. Diagnostic outputs alreadywork. Goalactive; no heartbeat/no timedrest.

Raw-demo continuation:
- Added `camloop.inspect_job` readonlyCLI views summary/prompt/cam/checks/memory; exact paths+hashes, missing historical files explicit. Tests2 added, latest inspect/live/telemetry9passed.
- Astra worker now persists `.request.instructions.txt` and `.request.execution.json` before inference. Live job persists `saved-memory.json` after remember. No model/verifier behavior changed.
- Fresh rehearsal04 job3f1703103c1f completed:2passing25.852s candidates,80.745ref,memory12->13,trace01a09950-08eb-78f2-886c-22c645c1774a readback. Exactinstruction/modelsettings+checks+fullplan+memorysnapshots inspected and saved rehearsal-04/*-inspection.json and raw-evidence-receipt.json. Requestedmodelgpt-6-astra fast low confirmed recorded.
- REHEARSAL.md documents rawCLI, distinction supplied developerinstruction vs providersystemprompt, structuredstrategy vs compiledmoves.
- Currentserver87155 alive. qa tab2 nowjob3f1703103c1f markedDeliverable; sponsorNowtab5Signals markedHandoff. Allcurrentjobscomplete. Goalactive, no timersrest. Next can pursue actualSignals runtime with Astra constraint or meaningful broader learning experiments/visual refinements; do not repeat alreadyverifiedpublications.

Repeated-learning study active:
- Launched `repeated_memory_study.py`, exec16990, log repeated-memory-study.log.12newAstrafastlow proposals+simulations:3repetitions×2existingbenchmarkparts(bearing,cage)×cold/warm, alternatingarmorder. Frozenmemoryfrom02manifest loaded snapshot contains01actuatoronly. No memoryupdatesbetweenstudytrials; samebaselinefeedbackbotharms. Scope selected-parttransfer, notunseenparts.
- Everytrialrecord persisted, errorsretained; resume skips only recordedcases. Never rerun solely for observationtimeout. `report_repeated_memory.py` prepares complete6pairsummary after all12; notyetexecuted.
- Added film/NARRATION.md timedspoken116secscript. Filmstill silent; intendedusernarration. No newmoviebuildneeded.

Repeated study complete:
- exec16990 terminalsuccess;12freshAstrafastlow proposals+fullsimulations,allpass. Sixpairs: warmwins3(cage),coldwins2(bearing),tie1(bearing). Cage196.77267warmall3vs360.00293/197.77511/197.88481cold. Bearing124.16424tie, warm130.22066vs125.52484/126.79667. Aggregate1132.14860coldvs974.92357warm is outlierdominated; do notselluniversal13.9%gain.
- repeated-memory-study/result.json,summary.json,REPORT.md,paired-comparison.png,integrity.json. 12targethashesandmodelsettingschecked. Frozenmemoryfromactuatoronly,selectedexistingbenchmarkparts,notunseenCAD. No studyfeedbackupdatesmemory.
- Publishedsix-pairdataset+2scoredarms: weave-publication.json cold01a09957-0623-74bb-a4f9-0454f87562da,warm01a09957-1147-79d2-98d6-6ccf6ee23aa2,readbackcomplete. Scope retainedscoresofnewexperiment, notfreshsimsinsidepublication.
- Actual thirdARIAmessage sent sameconversation withresults,outlier,movementhashfindingandfailedrefinement. sponsorNow/tab5 queryactivelythinking atlastobserved. Retrievefinalcritique beforeacting. MarkedHandoff.
- Filmupdatedcentralfailureloop: actualmanifoldreferencefail→Astraproposal→pass158.98s plusfreshmemoryaudit. render_failure_loop.py inspectedclean. Repeated6pairchart inserted8s, sleeveclipshortened8s; film/silta-demo.mp4remains115.96s3479frames,decodecheckedandcontactsheetinspected. NARRATION.md updated.
- SUBMISSION_DRAFT.md localonly2sentencecopy+sponsorusage+limits. No external submission.
- Currentserver87155 unchangedalive; allPDFjobscomplete. Goalactive, no blockers threshold/no resttimers. NeedactualSignalscompatibleAstra/runtime; ARIAreply; furtherheldoutapplicabilityexperimentguidedbycritiqueandvalidation; finalpresentationpolish as needed.

New-geometry continuation:
- ARIA thirdreviewcompleted,savedaria-repeated-study-review.txt. Confirmswarmnarrowsproposaldistribution,cageoutlierdominatesandbearingregresses. Recommendedpreregisteruntouchedsimilar/dissimilargeometries. ItsseedrecommendationcannotbesatisfiedbyCodextransport; explicitunseededlimitationrecorded.
- Askeduserasync whetherWeaveServerlessInference exceptionallowedforSignalsonly; earlierAstra-onlyinstructionotherwiseblocksusingnonAstraruntime. Questionpending, no dependent action. DefaultAstra-onlyretained. Otherworkproceeds.
- prepare_new_geometry_study.py froze11-indexed-instrument-shell and12-three-pocket-plate andpre-registrationbeforeproposals. new_geometry_memory_study.py exec83662completed12newAstrafastlow+simulations,allpass. Frozenactuatormemoryunchanged. Shellcold172.7383/172.8129/170.9840vswarm172.8636each; platecold34.67624/34.67624/34.68161vswarm34.67624each. No usefulgeneraltransfergain; medianwarm-cold shell+0.07254%,plate0. Integritymodelsettings,targethashes,andpreregistermtimebeforeallrequestscheckedinreport_new_geometry_memory.py; result/summary/REPORTundernew-geometry-memory-study.
- Publishednew-geometry-cam-memory-six-pairs+2arms,exec56034completedreadback;exactIDsweave-publication.json. No memoryrulepromoted.
- Exportedpreselectedfirstwarmtrialbothparts viaexport_transfer_previews.py,exec95013completed. Derivedworkspace-transfer-previewmanifestsstatusretained_trial,bestNone,explicitselectionattempt-0. Assetreplaysboundchecked. Newtab6transferViewshows11shellFinishedstock,visuallyverifiedandmarkedDeliverable.
- Post-runstockdiagnostics usingretainlower/uppermasks,noverifiersourcemutation: diagnose_retained_stock.pyexec94151complete. Manifoldoffpossiblemaxexcess0..3.768mm34possiblecells0definite; on0..1.945mm0possible; cage.95upper19.946mm1066possible0definite. STOСK_DIAGNOSTICS.md+stock-diagnostics.json includesregions/hashes. Analyticenvelopecheckpassedusingpropervenv. Noresolutionrefinement/physicaldefect/newgateclaim.
- demo_preflight.py passes58machinemeshes+groupvertexindices,alloriginalUMC750provenancehashes,completedlivejobtarget+trace,33playbackframesbinarybounds,filmhashduration. Receiptpreflight.json. Thisisreadinessnotnewmachining.
- Combined24plan4parttransferoverviewrendered/visuallyinspected,render_transfer_overview.py. build_film.py nowincludesit8s replacingselected6pairchart; NARRATIONupdatedtoincludeuntouchednegativefinding. Rebuildexec34987activeatcheckpoint;pollthenreview/decode/promotefilm/silta-demo.mp4andrefreshpreflight,don'trestartontransient.
- Server87155stillalive,PDFjobscomplete. qa tab2latest3f17; sponsorNowtab5ARIAcompletedreviewmarkedHandoff; transferViewtab6newpartmarkedDeliverable. Goalactive,no resttimers. Meaningfulprogresscontinues; pendingSignalsmodelchoiceisnotwholegoalblocked.
- Rebuild34987completed. Updatedfilm promotedtosilta-demo.mp4,3479frames115.96s,fulldecodepasses;receiptupdatedandpreflightrerunpasses. Combinedoverviewitselfvisuallyinspected; currentcontactsheetregenerated. No processeswaitingexceptpersistentlivewebserver.

## Live incumbent recovery and fifth traced rehearsal

Fixed the live optimizer to seed the incumbent with a passing reference, retain a newly verified candidate before playback export, return compile failures to CAM, and expose the retained manifest after an interruption. Invalid compilations cannot reach simulation or the timing judge. Missing compiled-plan downloads return a controlled 404. UI labels distinguish the best completed playback and a retained result after interruption; versioned module loading was visibly checked.

Full suite: 75 passed in 90.35 seconds. Four new orchestration regressions cover compile repair, slower candidate/reference comparison, later planner failure, and playback export failure.

Fresh updated-server PDF rehearsal: `99006bbab905`, Instrument Pocket, 125.518 reference → 27.2639827254 best estimated seconds; three passing proposals; memory 13 → 14. Actual Weave trace `01a0996a-bbd5-764e-b4b6-dbd1f554ab1b` completed readback. `rehearsal-05/fresh-live-rehearsal.json`, terminal tape, and preflight receipt retained. Browser playback and final pocket geometry visually inspected. This is another reliability rehearsal, not a new controlled learning experiment.

Consolidated START_HERE.md, corrected the three-minute script to start only one job at the beginning, and kept the 24-plan negative/mixed transfer conclusion explicit. Native Signals runtime question remains pending. Goal continues; no timers or recurring wakeups created.

## Captioned film and source-delivery audit

Replaced the opening text card with a visually inspected diagram of the implemented CAM/check/simulation/judge feedback paths. Added 19 selectable English caption cues so the silent film is understandable without live narration. `build_film.py` now calls `finalize_film.py`: embeds captions, reads every caption back, decodes the complete video, records source hashes and clip timing, and replaces the final video only after validation. Rebuilt film remains 115.96 seconds / 3479 frames. Updated-server preflight passed against the new film hash.

Fetched and inspected Joel's `origin/toukoversion` at 6984406: latest changes concern scripted demo timing and connected numbered-part motion. These are not additional simulator measurements.

Live GitHub metadata confirms the repository is public. However, `cnc_simulator/` is untracked locally; repository visibility does not deliver the current simulator implementation. Prepared `release/simulator-source.tar.gz` with 60 allowlisted source/test/example/documentation files and a per-file hash manifest, excluding private runs, credentials, vendor assets and environments. Extracted archive smoke test imported the extracted engine, verified every hash, and passed the pocket at 12.2 modeled seconds using the existing dependency environment. This is a source-review handoff, not a clean dependency install, published repository change, or full live-demo distribution. Delivery gap is explicit in START_HERE and SUBMISSION_DRAFT.

Goal remains active. Native Signals runtime clarification still pending; no changes to existing team's signal and no unapproved alternate model runtime.

## Fresh dependency environment and independent live run

Created a new Python 3.12 virtualenv under `competition/release/clean-venv`, installed the extracted package with test/report/agent/live extras, and ran 71 engine/loop tests successfully (104.68 seconds). The four legacy VF2 provenance tests require parent-repository assets and were explicitly excluded; they passed in the original 75-test workspace suite. Ten targeted live bootstrap/recovery tests also pass on the latest extracted source, including two newly added first-run memory cases.

Fixed fresh boot: absent study history now produces version-0 empty memory; existing memory is preserved. Added configurable local port. Expanded the allowlisted archive to 79 files (~9.5 MB): source, tests, example CAD, live viewer, local vendor modules, UMC geometry/provenance, and HANDOFF.md. No recorded runs, credentials, or environments are included.

Started the extracted service on 2745 (exec 6011), leaving the main traced service at 2744 unchanged. Browser-driven Try example → coordinate review → run completed as `d6c0b4356c2b`: reference 80.7454447 → best 25.8522363 estimated seconds, two passing attempts, empty memory 0 → 1. Verified every packaged file hash, unchanged target, and served playback buffers. Visually inspected the generated finished pocket. Receipt: `release/clean-live-receipt.json`; logs/dependency list retained. Weave intentionally unconfigured for this independent installation; primary service remains traced. This verifies handoff execution, not memory transfer or physical machining.

Source archive has not been published or committed to the shared repository. The archive can now reproduce a fresh live run with an authorized Astra account; recorded studies and the film remain separate artifacts. Goal continues.

## Finished-stock framing and replay transitions

Finished stock now frames the remaining mesh's world-space bounds, including its final B/C transform and viewport aspect ratio. The camera fits both the simple pocket and indexed shell without changing geometry or verification. Visually checked both in the browser; saved `finished-shell-view.png` from the actual viewer.

Fixed Play and seek transitions out of Finished stock: they now restore machining mode and visible cutter. Seeking preserves the requested time before view changes update controls. Browser checks verified restart at 0:00 with visible tool, and keyboard seek from final time back to the requested start position. JS syntax checks pass. No simulator/model changes.

Updated the 79-file handoff archive and extracted live viewer. All source hashes and existing clean-run target/playback checks pass. The clean live machining run predates these viewer-only changes; it is not claimed as newly rerun. Existing film remains the verified earlier capture; new framing applies to interactive replay and future recordings. Goal remains active.

## Audited memory continuity in actual CAM requests

Revisited official Weave documentation and installed SDK for a deterministic native Signals path; no supported route was established. Existing Signals runtime clarification remains pending. No new native Signal or alternate model was configured.

Added `audit_memory_chain.py`: verifies loaded version-file bytes against the recorded hash, exact retained memory in the first CAM request, appended episode/version, and saved commit/snapshot hashes. All nine completed main-workspace jobs across two drawing types pass the persistence/input checks; all eight consecutive links verify. Seven older jobs lack an extra per-job saved-memory copy; their canonical versioned files and commit receipts are present. No missing historical artifacts were backfilled.

Regression test passes: modifying the first CAM memory or stored version file makes the audit fail, even with matching version counters. The audit is published to Weave with completed readback, receipt `memory-chain-weave.json`, explicitly labeled retained-artifact audit (no new model, simulation, or native Signals execution). START_HERE links the local report. This strengthens evidence of persistent context delivery; it does not revise the negative/mixed 24-plan transfer result. Goal continues.

## Isolated local Git handoff

Prepared branch `codex/programmatic-cnc-demo` in `/tmp/silta-programmatic-review`, based on main checkout HEAD 0afc2ec. Commit bb8640e954b2cdffc8825e147c4a5ec958ad6c44 adds 80 simulator-only files. The team's primary checkout, index and Fusion edits were not included or modified. The review worktree is clean. No push, merge or event submission.

The default standalone `pytest -q` command now passes all 73 tests in a fresh dependency environment (89.42 seconds). The source archive explicitly omits only the older VF2/Fusion provenance module requiring files outside the standalone package; that original module remains unchanged in the main workspace. Viewer vendor files are explicitly tracked despite the development ignore rule. PDF/STL/F3D assets have binary Git attributes; original vendor source whitespace is preserved.

All 78 archive files match staged content. Known secret-format scan found no matching files; this is a bounded pattern check, not universal certification. Created `release/programmatic-cnc.bundle` and `programmatic-cnc.patch`. Git verified the bundle; it requires parent 0afc2ec. Applied the binary patch into an isolated temporary Git index and verified that it reconstructs the exact committed tree. Receipt and draft PR description are under release/. Public GitHub delivery remains pending; the source handoff is now concrete and reviewable. Goal remains active.

## Resolution refinement in progress

The earlier 0.5 mm experiments were blocked by the default two-million-cell cap. Exact preflight counts are 2,658,024 manifold cells and 3,466,008 cage cells, both within a four-million-cell cap and the simulator's supported maximum. Started `refine_uncertain_cases.py` (exec 78785, observed PID 45462) with those two predeclared cases, 0.5 mm cells and max_cells=4,000,000. No other plan, target, fixture, feed, tolerance or verifier changes. Pre-registration and frozen refined plans are under `resolution-refinement/`; original results remain intact.

At the latest observation the first case was still computing, CPU active, RSS under 0.9 GB; there is no refined verdict yet. Poll the same exec handle or inspect authoritative process/log state before deciding it stopped; do not restart on observation timeout. The script saves each completed result incrementally and then proceeds to the second case. `report_resolution_refinement.py` is ready and requires both outcomes before reporting. This is verification-resolution evidence, not a new CAM optimization or timing speedup.

A direct contract check using actual saved manifold failure evidence confirmed: the original matching contract triggers the exact failure check; changing resolution/cell budget makes it abstain. Receipt `resolution-refinement/memory-contract-check.json`. No new memory policy or model calls. Goal continues.

## Completed: three complex indexed parts

All three frozen targets completed reference simulation and two Astra CAM revisions each; all nine plans passed the configured gates. Carrier: 1984.7 → 496.8 s (26 features, 13 orientations); manifold: 821.1 → 309.4 s (19 features, 5 orientations); staggered drum: 1742.7 → 444.1 s (26 features, 17 orientations). Memory versions progressed 14 → 15 → 16 → 17. This is a complexity stress test, not a controlled memory-benefit result.

Open http://127.0.0.1:2746/study.html for individual videos, raw CAM attempts and interactive stock replay. The fully decoded 89.96-second reel is `../workspace-complex-three/videos/three-complex-parts.mp4`; full results are `../workspace-complex-three/RESULTS.md`. Weave reference/best evaluations were read back complete; receipt: `../workspace-complex-three/weave-publication.json`. Geometry uses indexed 3+2, 1 mm cells and 3 mm demo tolerance; time is a constant-speed estimate.

Resolution-only follow-up completed: at 0.5 mm cells and a 4M cell cap, the fixed manifold plan passed (116.08 s); the fixed cage plan remained unresolved and failed the gate (200.78 s). No CAM, target or tolerance changes. Original outcomes retained. See `resolution-refinement/REPORT.md`.

Main film refreshed with all three complex-part captures and direct-source timing chart; 115.96 seconds, 19 embedded captions read back, full decode passed. Film SHA256: fb35174962261cd0d867f5bc8b941c3c59060557531d983cf3ec530516d9d107. Caption/chart distinguish reference speedup from carrier further improvement, manifold tie and drum regression with best retention. Retained-artifact iteration audit: `../workspace-complex-three/iteration-audit.json`. Original film preserved as `film/silta-demo-before-complex-parts.mp4`. Goal remains active; native Signals and external submission/access gaps remain open.

Film visual QA removed an unsupported arrow glyph in the results panel; current final-film SHA256 is 4eee8f3a5de11f29351f6564ccf61250837dc6e570c90b950fdc2c58052f44cf. Receipt remains the authoritative current hash.

Reproducible complex-part handoff added: standalone archive now has 83 allowlisted files, including study viewer, capture encoder/server, and three-part runner. Runner defaults to memory 0, accepts explicit seed, supports CAD-only preparation, and refuses overwrite. A fresh archive extraction prepared all three targets without model calls, verified zero-history startup and overwrite rejection; independent pocket smoke passed at 12.2 s. Local branch commit `85415e0` includes 85 paths from the parent, and regenerated bundle/patch reconstruct the exact commit tree. No push or merge. Goal remains active.

Fetched GitHub again: Joel’s origin/toukoversion remains 6984406; no new commits to integrate. Hardened raw terminal observation against transient HTTP errors and partially persisted attempts, added explicit port selection and truthful unavailable-state reporting. Two targeted regressions pass. Actual job 99006bbab905 readback completed and preserved all three verdicts/timing decisions; this was not a new machining run. Current preflight for that job validates frozen CAD, downloaded 58-mesh UMC, stock replay and latest film. Goal continues.

Portable demo kit built at `release/silta-demo-kit.zip` (26 files, about 22 MB). Includes current film, complex reel, raw outcomes/iterations, ARIA review plus 12-case results, hosted-check receipt, persistence/transfer reports and runnable source archive. Every archive member hash and every START.md link verified. Local draft updated with new complex results and within-run outcomes. No external submission or publication. Goal active.

Added three analytical midpoint-only indexing collision cases: stock/cutter, fixture/cutter and fixture/holder. Both endpoint poses independently pass, while each C=0→180 sweep has definite move-0 collision and preserves stock. All eight indexing tests pass (`indexing-safety-tests.xml`). Engine code unchanged. Refreshed source archive and portable kit include these tests; the existing local Git review commit remains the earlier source snapshot. Goal active.

Publication prepared at isolated commit `a054a16`: 87 simulator-only paths, including current rotation safety tests and resilient terminal observer. Public push/draft-PR approval requested explicitly; no remote write performed. Known credential-pattern scan has zero matches (bounded scan). Full isolated suite running, process 47280. Source archive now has 85 files; portable kit refreshed. Goal remains active.

Current isolated publication branch full suite completed: 78 passed in 91.46 seconds; only existing SWIG deprecation warnings. Git bundle and patch refreshed to a054a16 and exact tree reconstruction verified. Public push/draft-PR permission still pending; no publication performed. Goal active.

Rechecked live organizer listing: September 13 loop awards and Sunday 1 PM submission / 1:30 PM judging confirmed. Native Signals is not stated as a prerequisite on the public listing; full private rubric still not refreshed. Added evidence-specific judge Q&A covering actual iteration vs baseline, negative transfer, numerical uncertainty, indexed scope, ARIA ablation and hosted-check limits. Publication approval and actual submission remain outstanding; goal active.

Fresh end-to-end browser rehearsal 469436bb78a7 completed with two passing tied proposals, best 25.852 s vs 80.745 s reference, main-workspace memory14→15. UI reviewed through dimension confirmation, CAM and actual replay/final stock. Trace readback + current preflight pass; terminal event tape retained under rehearsal-06. No new causal learning claim. Goal active.

Inspected actual AGI House event UI: September 12 title/date and current tracks confirmed. Submission fields are not exposed while signed out. Sign-in offers email magic link or Google; unlocked Bitwarden contains no AGI House match. Requested existing registered email and final roster rather than guessing or creating an account. No registration/submission/login-link action taken. Publication and native-Signals choices still pending. Goal active; remaining steps require user information/approval.

Blocked-input audit: no active main-demo jobs remain; prepared publication commit a054a16 is unchanged. Across three consecutive final-readiness turns, source-publication approval, Signals runtime choice and submission account/team information remain unavailable. No additional aligned local work was identified beyond the verified demo, evaluations, tests, film and portable handoff. Marking goal blocked on those inputs, not complete.
