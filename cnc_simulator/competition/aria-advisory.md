# Tested outer-loop advisory

Source: actual ARIA follow-up saved in aria-followup-response.txt. Selected experiment: aria-ablation/result.json, twelve independently executed simulator calls saved locally; published evaluations replay retained results and are not fresh simulator executions.

Keep the existing exact-identity failure gate. Do not promote a universal cleanup or stepover rule. The manifold cleanup-off path remains unresolved at the demo grid; adding cleanup passes this fixed target/setup. Cage cleanup changes no generated movements and does not rescue its 0.95 case.

Follow-up audit: movement-hashes.json proves the three manifold stepover settings produce identical movements within each cleanup arm (330 vs 344 moves). They are three requested parameter settings, but only two distinct effective manifold paths. Cage has three distinct effective paths; cleanup is a no-op. Thus the twelve-case ablation contains five distinct paths, not twelve independent geometric strategies.

Attempted 0.5 mm adjudication in resolution-audit/result.json hit the configured grid resource limit for both cases. This does not resolve the geometry uncertainty and does not create a pass. No physical defect claim is supported.

Useful retained benefit: reject already measured exact failed paths before repeating full simulation. Changed setup requires full verification. Cold/warm transfer evidence remains mixed.
