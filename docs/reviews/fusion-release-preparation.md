# Fusion release preparation — current snapshot

Updated September 13, 2026. This isolated source worktree is prepared for review;
no final commit, push, submission or final evidence bundle has been made here.

## Scope and preservation

Source: `coreweavehack`; release branch: `fusion-release-prep-20260912`, based on
`6984406`. The JSON snapshot beside this file is the authoritative file allowlist,
byte hashes, resync history and protected-file record. All 12 protected remote
files still match their recorded hashes. Latest allowlisted source drift: none.
Protected Joel work remains intact; runtime outputs are not copied into this
source-only worktree.

## Current outcome and attribution

All nine indexed campaign rows completed. Four historical parts bring the selected
historical-plus-campaign report to 13 completed distinct drawings; the separate
finned rehearsal brings the CAD gallery to 14. Retries are not additional parts.
The six-part chart covers UMC04/05/06/08/09/12 with source-matched verified pairs.
The separate UMC11 chart labels its44.42% reduction as an operator-nominated
retained-CAM retest, not autonomous discovery or new learning. UMC03's fresh v7
comparison is438.955225→433.128113s; broader mixed-verifier history is explicitly
marked. Old partial-stock UMC11 verdicts remain preserved and collection-invalidated.

## Synced changes

Reports, nomination attribution tests, gallery, claims/runbook, package source,
controller/resumer incumbent handling, v7 stock-regeneration completion gating,
and bounded untitled/fading playback-menu recovery are copied with source hashes.
UMC12 portable-v2 capture proof freezes all six original implementation sources;
its actual operator-assisted rewind remains disclosed. The final cage footage,
film-tail render, fresh stage rehearsal and final package are owned by the root
worker and were not fabricated or published by this sync.

## Validation and remaining release gates

Final full isolated suite reported by root: **550 passed, 7 upstream warnings in5.50seconds**;
no skipped tests reported. This audit did not rerun tests. Current selected source
and release hashes were freshly reconciled:14 previously stale entries already
matched byte-for-byte; the final viewer-label source/test update was then reconciled
in the same way. No source was copied. All12 protected hashes remain
unchanged; current source drift is empty. `git diff --check` passed in the release
worktree. Earlier validation results and prior file hashes remain in JSON history.

The114-second final film is complete with source-bound cage/clevis stock orbits;
its decoded first104seconds remain unchanged. Live-stage status is documented in
the current runbook and actual receipts, independently of that recorded film.
Root owns completion of the fresh rehearsal, final evidence-package review and
repository delivery. No Fusion actions, staging, commit or publication occurred
in this audit.
